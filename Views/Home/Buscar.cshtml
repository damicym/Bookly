<!-- @model IEnumerable<Bookly.Models.Libro>

@{
    ViewData["Title"] = "Resultados de búsqueda";
}

<h2 class="busqueda-titulo">Resultados de búsqueda</h2>

<form method="get" asp-action="Buscar" class="busqueda-form">
    <input type="text" name="query" placeholder="Buscar libro..." value="@ViewBag.Query" class="busqueda-input" />
    <button type="submit" class="busqueda-boton">Buscar</button>
</form>

<div class="busqueda-grid-container">
    <div class="busqueda-grid">
        @if (!Model.Any())
        {
            <div class="no-result">
                <p>No se encontraron resultados.</p>
            </div>
        }
        else
        {
            foreach (var libro in Model)
            {
                <div class="busqueda-item">
                    <div class="busqueda-card">
                        <img src="@libro.ImagenUrl" alt="@libro.Nombre" class="busqueda-img">
                        <div class="busqueda-card-body">
                            <h5 class="busqueda-card-title">@libro.Nombre</h5>
                            <p class="busqueda-card-text">@libro.Autor</p>
                            <a href="@Url.Action("Detalles", "Libros", new { id = libro.Id })" class="busqueda-card-link">Ver detalles</a>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div> -->